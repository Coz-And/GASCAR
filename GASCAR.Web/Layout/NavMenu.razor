

@inject GASCAR.Web.Services.AuthStateService AuthState
@inject NavigationManager Nav


<div class="top-row ps-3 navbar navbar-dark shadow-sm">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <button class="btn btn-link navbar-brand fw-bold p-0 m-0 text-white" style="text-decoration:none;" @onclick="NavigateHome">
            <span class="bi bi-lightning-charge-fill me-2"></span>GASCAR
        </button>
        <div class="d-flex align-items-center">
            @if (!AuthState.IsAuthenticated)
            {
                <NavLink class="nav-link text-white" href="/login">
                    <span class="bi bi-box-arrow-in-right"></span> Login
                </NavLink>
                <NavLink class="nav-link text-white" href="/register">
                    <span class="bi bi-person-plus"></span> Registrati
                </NavLink>
            }
            else if (AuthState.IsAdmin)
            {
                <a href="/admin/edit-profile" class="btn btn-outline-light btn-sm me-2">Modifica dati admin</a>
                <button class="btn btn-outline-light btn-sm" @onclick="Logout">Logout</button>
            }
            else
            {
                <button class="btn btn-outline-light btn-sm" @onclick="Logout">Logout</button>
            }
        </div>
        <button title="Navigation menu" class="navbar-toggler ms-2" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>


<div class="@NavMenuCssClass nav-scrollable bg-dark text-white" style="min-height:100vh;">
    <nav class="nav flex-column py-3">
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link text-white" href="/">
                <span class="bi bi-house-door-fill"></span> Home
            </NavLink>
        </div>
        @if (!AuthState.IsAuthenticated)
        {
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/login">
                    <span class="bi bi-box-arrow-in-right"></span> Login
                </NavLink>
            </div>
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/register">
                    <span class="bi bi-person-plus"></span> Registrati
                </NavLink>
            </div>
        }
        else if (AuthState.IsAdmin)
        {
            <div class="nav-item px-3 mb-2">
                <a class="nav-link text-white" href="/admin/edit-profile">
                    <span class="bi bi-pencil-square"></span> Modifica dati admin
                </a>
            </div>
            <div class="nav-item px-3 mb-2">
                <button class="nav-link text-white btn btn-link p-0" @onclick="Logout">
                    <span class="bi bi-box-arrow-right"></span> Logout
                </button>
            </div>
        }
        else
        {
            <div class="nav-item px-3 mb-2">
                <button class="nav-link text-white btn btn-link p-0" @onclick="Logout">
                    <span class="bi bi-box-arrow-right"></span> Logout
                </button>
            </div>
        }
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link text-warning" href="/admin">
                        <span class="bi bi-shield-lock"></span> Dashboard Admin
                    </NavLink>
                </div>
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link text-warning" href="/admin/stations">
                        <span class="bi bi-ev-station"></span> Colonnine
                    </NavLink>
                </div>
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link text-warning" href="/admin/edit-stations">
                        <span class="bi bi-pencil-square"></span> Modifica Colonnine
                    </NavLink>
                </div>
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link text-warning" href="/admin/transactions">
                        <span class="bi bi-cash-coin"></span> Transazioni Utenti
                    </NavLink>
                </div>
                <div class="dropdown-divider my-2"></div>
            }
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/dashboard">
                    <span class="bi bi-speedometer2"></span> Dashboard
                </NavLink>
            </div>
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/notifications">
                    <span class="bi bi-bell"></span> Notifiche
                </NavLink>
            </div>
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/cars">
                    <span class="bi bi-car-front"></span> Le tue auto
                </NavLink>
            </div>
            <div class="nav-item px-3 mb-2">
                <NavLink class="nav-link text-white" href="/transactions">
                    <span class="bi bi-list-nested"></span> Transazioni
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;


    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private void Logout()
    {
        AuthState.Logout();
        Nav.NavigateTo("/");
    }

    private void NavigateHome()
    {
        Nav.NavigateTo("/");
    }
}
